# Day-05

Today I have learnt about CSS modules and see how modules work 
if we have same class of a tag then it donot overlay and don't create
conflic because each component has its own private CSS.

